<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.fiap.eightshop">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EightShop">
        <activity android:name=".ui.product2.ProductListActivity2"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ProductRow" />
        <activity android:name=".ProductListActivity">
        </activity>
        <activity android:name=".HallRow" />
        <activity android:name=".HallListActivity">
        </activity>
        <activity android:name=".CustomList"/>

        <activity android:name=".ListaTest">
        </activity>
        <activity android:name=".MainActivity"/>
        <activity android:name=".ui.product2.ShoppingCartActivity"
                  android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".SignUpActivity" />
        <activity
            android:name=".ListMerchantActivity"
            android:label="@string/title_activity_list_merchant"/>

        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/app_name" />
    </application>

</manifest>