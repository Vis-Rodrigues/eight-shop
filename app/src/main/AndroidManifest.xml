<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.fiap.eightshop">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EightShop">


        <activity android:name=".HallRow"></activity>
        <activity android:name=".HallListActivity"/>
        <activity android:name=".CustomList" />
        <activity android:name=".ListaTest">

        </activity>
        <activity android:name=".MainActivity" />

        <activity
            android:name=".LoginNova"
            android:label="@string/title_activity_login_nova"
            android:theme="@style/Theme.EightShop.NoActionBar"></activity>
        <activity android:name=".Hall" />

        <activity android:name=".SignUpActivity" />

        <activity
            android:name=".ListMerchantActivity"
            android:label="@string/title_activity_list_merchant" >

        </activity>

        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/app_name">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
    </application>

</manifest>